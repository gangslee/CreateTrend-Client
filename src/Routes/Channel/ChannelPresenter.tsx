import React from "react";

interface IChannelPresenterProps {
  loading: boolean;
}

export default function ChannelPresenter({ loading }: IChannelPresenterProps) {
  return <h1>ChannelPresenter</h1>;
}
